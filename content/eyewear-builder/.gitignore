node_modules
dist
*.tsbuildinfo

# Machine-local tool config (the Impeccable design hook writes this).
.claude/settings.local.json

# Generated by scripts/sync-wasm.mjs from @mediapipe/tasks-vision.
# 34 MB, and a checked-in copy goes stale the moment the dep is bumped.
public/wasm/

# Screenshots dropped in here while reviewing. They are notes to a
# conversation, not to the repo.
Screenshot*.png
